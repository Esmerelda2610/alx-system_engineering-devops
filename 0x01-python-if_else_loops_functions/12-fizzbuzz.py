#!/usr/bin/python3
# Author - ELIZABETH MOTSINONE
""""print the numbers from 1 to 100 separated by space.
    For multiples of three, print fizz instead of the number.
    For multiple of five, print Buzz instead of number.
    For multiple of three and five, print FizzBuzz instead of number.
    """
    def fizzbuzz();
        for number in range(1, 101);
            if number % 3 == 0 and number % 5 == 0;
                print("FIZZ ", end=""')
            elif number % 3 == 0;
                print("FIZZ ", end="")
            elif number % 5 == 0;
                print("BUZZ", end="")
            else:
                print("{} ",format(number), end=""')
